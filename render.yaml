




services:
  - type: web
    name: jobot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: DB_PATH
        value: /opt/render/project/src/data/jobot.db