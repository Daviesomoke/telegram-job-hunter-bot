






# FIX: unpinned versions (>=3.0) can pull breaking updates on every deploy.
# Pinned to known-good versions for reproducible builds.
aiogram==3.7.0
aiosqlite==0.20.0
aiohttp==3.9.5
feedparser==6.0.11
apscheduler==3.10.4
python-dotenv==1.0.1