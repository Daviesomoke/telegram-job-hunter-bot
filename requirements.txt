







# Core bot framework
aiogram==3.7.0

# HTTP client (used by aiogram + reddit scraper)
aiohttp==3.9.5

# Async SQLite
aiosqlite==0.20.0

# RSS feed parsing
feedparser==6.0.11

# Job scheduler
apscheduler==3.10.4

# .env file support
python-dotenv==1.0.1

# Pydantic — pinned explicitly so pip uses pre-built wheels
# and never tries to compile pydantic-core from source with Rust/Cargo
pydantic==2.10.6
pydantic-core==2.27.2